- hosts: all
  tasks:
    - name: Run testinfra validation
      include_role:
        name: tox
      vars:
        tox_envlist: venv
        tox_extra_args: -vv molecule verify
        tox_install_siblings: false
